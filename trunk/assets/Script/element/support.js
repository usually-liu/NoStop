cc.Class({
    extends: cc.Component,

    properties: {
        
    },

    // LIFE-CYCLE CALLBACKS:

    // onLoad () {},

    start () {
        //直接控制支援小动物的动画播放
        // let anim = this.getComponent(cc.Animation);
        // let animState = anim.play('support Move');
        // animState.wrapMode = cc.WrapMode.Loop;
    },

    // update (dt) {},
});
